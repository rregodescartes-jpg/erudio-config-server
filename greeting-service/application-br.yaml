spring:
  application:
    name: greeting-service
  cloud:
    config:
      enabled: false
greeting-service:
  name: "Rafael"
  defaultValue: "Bem-vindo!"

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
  endpoint:
    health:
      show-details: always
